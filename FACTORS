#include <stdio.h>
int main() {
    int num, i;
    printf("enter numebr: ");
    scanf("%d", &num);
    printf("factor of %d : ", num);
    for (i = 1; i <= num; i++) {
        
        if (num % i == 0) {
            printf("%d ", i);
        }
    }
    return 0;
}
